package com.myProject.ShopShopDay.repository;

import com.myProject.ShopShopDay.model.VisualTag;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VisualTagRepository extends JpaRepository<VisualTag, Long> {
}
